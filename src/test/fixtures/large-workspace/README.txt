Use this directory as the seed for a generated large-workspace test fixture.
During the responsiveness test, copy the seed source file into a temporary workspace
until the fixture reaches at least 500 files, then assert that batch yielding keeps
the extension responsive while indexing proceeds.
